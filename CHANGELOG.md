# v2.2.7 更新内容

## 更改

- 签到信息：获得好感度 / 获得宠物经验 改为显示**最近一次签到**产生的变更量（原先为历史累积值，显示失真）；每次签到先清零再累加（双倍签到多次结算 = 本次签到总额），与「获得金币」口径一致
- WebUI「运行记录 → 宠物记录 → 宠物详情」属性变化记录：条目展开后显示本次使用的道具及 ×N 数量（使用道具 / 自动照顾；自动照顾的道具使用完整记录，含体力丸），道具效果按使用当时数值冻结存档
- WebUI 宠物详情：鼠标悬停道具名称时，属性条变化区高亮该道具作用的属性，并在数值旁显示作用效果
- WebUI「运行记录 → 商店价格」：窗口记录改为保存整店商品快照（含原价商品），点击窗口卡片展开「当时商店信息」，行格式与当前窗口卡片一致（打折商品显示 原价 → 现价（X 折），原价商品直读）
- 帮助菜单（签到帮助 / 宠物帮助 / 左轮手枪帮助 / 农场帮助 / 游戏帮助）改为卡片渲染：每个模块的指令放入一张独立卡片，长文本自动换行，输出图片遵循长宽比要求

## 修复

- WebUI「运行记录 → 宠物记录」的自动化贷款「豁免贷款」按钮修复（第二次）：插件 Page 由 Dashboard 以受限 iframe 加载，`window.confirm` 会被静默拦截（不弹窗且直接返回 false），导致点击豁免后确认流程永远走不到、贷款清零从未执行——现改用页内确认弹窗（`uiConfirm`）代替原生 confirm，历史配置「删除版本」的同根因一并修复；点击链路本身（豁免按钮 → 接口 → 详情页互不劫持）已通过 test_waive_click_sim.js 模拟验证
- WebUI「运行记录 → 宠物记录」的自动化贷款「豁免贷款」按钮修复：原实现点击豁免芯片会同时被宠物详情页点击事件劫持（跳转详情页、反馈被吞），现已排除豁免/开关芯片，点击仅执行豁免

## 重做（自动照顾 + 自动打工 + 自动化贷款 全部重写，旧自动化代码删除）

- 指令：`自动照顾 开` —— 开启自动照顾并同步开启自动打工，随后执行开启时的初始照顾（使用道具将宠物所有属性提升到第 1 档，健康补到目标值，记录使用了多少金币）；`自动照顾 关` —— 关闭自动照顾并同步关闭自动打工
- 道具来源：优先使用用户持有的道具（五条属性效果全部生效并套用好感加成，与手动使用一致），仓库没有再购买并立即使用；自动化购买价格 = 用户手动购买价格 × 1.1 倍（WebUI「设置 → 宠物 → 自动照顾」可调）
- 触发逻辑：开启后，宠物活动（打工/玩耍）、使用道具（自动照顾自身产生的变化除外）、每日结算导致任意属性变化时自动触发检查
- 检查机制：任一属性处于第 3/4 档位 → 立刻自动照顾
- 道具使用机制：健康优先补到最大值的 80%（WebUI 可调），随后按 饱食(食物) → 口渴(饮料) → 心情(玩具) → 体力(体力丸) 顺序逐项检查——处于第 1/2 档忽略、第 3/4 档补满；体力丸仅自动化程序可购买（50 金币，直接补满体力）；使用道具前先检查仓库，没有再用金币购买
- 自动化贷款：贷款金额无上限、无逾期时间、无利息，仅能通过自动照顾进行贷款（不能手动贷款）；可用「还款」指令或自动打工报酬自动扣除的方式偿还
- 基准金币：自动照顾的金币消耗账单，也是自动打工的项目选择依据——自动照顾的道具金币消耗为加，自动打工的报酬入账为减（不论报酬进入金币账户还是偿还自动化贷款）
- 自动打工：触发条件 = 自动照顾开启 + 基准金币 > 0 + 用户指令或管理员后台开启了自动打工；流程 = 检查基准金币 → 选择报酬最接近基准金币的可进行项目 → 等待打工完成进入下一轮；自动照顾行为导致基准金币增加时即启动
- 统一数值管理：宠物档位划分不再由管理员单独配置，统一从判定属性最大值的数据（健康度对应属性上限）推导——一档 60%、二档 35%、三档 15% 上限，原「宠物结算·档位数值」配置项移除
- 兼容性：旧版本自动照顾/自动打工开关状态与基准金币直接继承；旧版自动化贷款账单自动迁移为新版贷款余额（按剩余本金并入，原账单清除）

## 删除

- 旧版自动照顾引擎（生效率过滤、失败冷却、定期巡检触发、自动打工经验收益等遗留机制）与自动化专属贷款套餐（单笔/总额上限、逾期天数、日息、获得金币自动扣还）全部移除
