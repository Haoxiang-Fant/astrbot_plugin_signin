# v2.3.0 更新内容

## 新增

- **权限管理（WebUI「设置 → 权限管理」）**：以 AstrBot 会话唯一标识 **UMO**（`平台:类型:会话id`）为聊天 sid，管理员可对每个聊天独立设置插件权限，三态：
  - **完全允许**（默认）：该聊天可完整使用插件全部功能，无需配置
  - **完全禁止**：插件在该聊天**完全不响应**（不展开同义口令、不记账、不回复）
  - **部分禁止**：管理员按功能勾选该聊天内不可用的功能（签到 / 宠物 / 农场 / 偷菜 / 红包 / 银行贷款 / 银行储蓄 / 左轮手枪 / 排行榜 / 自动化 / 活动系统 / 其他功能），调用被禁功能时提示「**该功能暂时关闭**」
- **聊天列表自动获取**：机器人收到消息的聊天（UMO）自动登记进列表（含已完全禁止的聊天，管理员始终可见可管理）；**「获取UMO」按钮**可手动从平台拉取机器人所在的全部群聊与好友私聊（OneBot/aiocqhttp 平台支持，补全群名/昵称）；仍支持手动添加 sid
- **安全鉴权**：每次使用插件功能前均做权限校验——消息入口统一拦截（完全禁止在消息处理最前直接静默，部分禁止在指令路由唯一入口按功能拦截），保存后立即生效、重启后保留
- 聊天权限标识 sid = UMO（`平台:类型:会话id`），与 AstrBot 会话标识同源，保证后台配置与运行时鉴权 key 一致

## 更改

- 「功能开关」新增 左轮手枪 / 排行榜 / 自动化 / 其他功能 四个全局开关（默认开启，行为不变）；全部插件指令（含管理员数据管理指令）均已归入对应功能维度，可被全局开关与聊天权限共同管控
- 聊天权限与全局「功能开关」叠加生效：先判聊天权限，再判全局开关
- WebUI「运行记录 → 商店价格」改为信息流瀑布布局：卡片宽度不再与同行卡片绑定、高度各自独立，按实际大小放入最矮列；展开/折叠时卡片间距保持不变，展开仅在原位增高、推挤同列下方卡片
- 商店价格窗口卡片展开的「当时商店快照」分门别类展示（与前台「商店」指令一致：分类内按实时价升序、分类间按组内最低价升序）；新记录附带商品类型，旧记录按当前商店配置回退归类
- WebUI 页脚版本号更新为 2.3.0，并新增「查看仓库」按钮（跳转 GitHub 项目仓库）
